---
apiVersion: kubeadm.k8s.io/v1beta4
kind: JoinConfiguration
discovery:
  bootstrapToken:
    apiServerEndpoint: "k8s.satriashp.site:6443" # API Server endpoint
    # token and caCerthasts that print after kubeadm init on control-plane node
    token: 0mlkgs.q59j4133j4s2ysh4 # change with appropiate value
    caCertHashes:
      - sha256:9f75fca9362fc367f871a5359cf830f4f9c4e349b04a3e6733f85fb81be19652 # change with appropiate value
# nodeRegistration:
#   kubeletExtraArgs:
#     # ip address of current node
#     - name: node-ip
#       value: 192.168.0.86

---
# Instead of self signing a serving certificate,
# the Kubelet will request a certificate from the 'certificates.k8s.io' API.
# This requires an approver to approve the certificate signing requests (CSR).
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
serverTLSBootstrap: true
