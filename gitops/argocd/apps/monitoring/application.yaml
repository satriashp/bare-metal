apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  project: bare-metal
  source:
    path: gitops/argocd/apps/monitoring
    repoURL: https://github.com/satriashp/bare-metal.git
    targetRevision: main

  destination:
    name: "in-cluster"

  syncPolicy:
    syncPolicy:
      syncOptions:
        - ServerSideApply=true
    automated:
      prune: true
      selfHeal: true
